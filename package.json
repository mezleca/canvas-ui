{
    "name": "canvas-ui",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "format": "prettier --write .",
        "dev": "bun run format && bunx vite --config vite.config.ts --host --port 3000",
        "test": "bun run format && bun test",
        "typecheck": "bunx tsc --noEmit"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "vite": "^6.1.0"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "prettier": "^3.8.1",
        "prettier-plugin-svelte": "^3.4.1"
    }
}
